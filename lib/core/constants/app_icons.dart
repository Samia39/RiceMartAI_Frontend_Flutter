import 'package:flutter/material.dart';

class AppIcons {
  AppIcons._();

  static const home = Icons.home;
  static const shops = Icons.store;
  static const rice = Icons.rice_bowl;
  static const add = Icons.add_circle;
  static const chat = Icons.chat;
  static const profile = Icons.person;
}

class BaseUrl {
  //static const String url = "http://ricemart.sandbox.pk/api";
  static const String url = "http://ricemart.sandbox.pk/api";
}
