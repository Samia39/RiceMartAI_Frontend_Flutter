name: sheezafrontendprojectricemart
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get: ^4.6.6
  get_storage: ^2.0.5
  http: ^1.5.0
  cached_network_image: ^3.2.3
  flutter_rating_bar: ^4.0.0
  shared_preferences: ^2.2.2
  image_picker: ^0.8.6

dev_dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
